{
  "name": "fleetx",
  "private": true,
  "scripts": {
    "backend": "cd backend && .\\run-backend.cmd",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
